# l'ambiente virtuale è quello di base (base), ho già installato tutto qua dentro (streamlit, pandas, plotly, requests)
import streamlit as st
import requests
from datetime import date, timedelta
import plotly.express as px
import pandas as pd
